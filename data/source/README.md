# Source DXF

Place the original drawing here as `original.dxf` before running the splitter.

The provided 41.4 MB sample is intentionally excluded from Git by default because the current ChatGPT GitHub connector cannot stream local binary files directly.
